# Configure and install the main script
gtranscribe_script = configure_file(
  input: 'gtranscribe',
  output: 'gtranscribe',
  copy: true,
  install: true,
  install_dir: bindir,
  install_mode: 'rwxr-xr-x'
)
